# 서비스데스크 Design Document

| 항목 | 내용 |
|------|------|
| 기능명 | 서비스데스크 (nu-ServiceDesk) |
| 작성일 | 2026-04-09 |
| 버전 | V2.1 |
| 상태 | In Development (Phase 1 구현 완료) |
| Plan 참조 | 서비스데스크.plan.V2.1.md |
| PRD 참조 | 서비스데스크.prd.V2.1.md |
| 선택 아키텍처 | Option C — Pragmatic Balance |
| 변경 근거 | 2차 전문가 6인 검토 + Bootstrap 5 전환 + 사용자 결정 반영 |

---

## Context Anchor

| 항목 | 내용 |
|------|------|
| **WHY** | 한국 기업 특화 서비스데스크 → 계층형 승인, 프로젝트 기반 티켓 관리, 자동접수/처리기한 자동 관리 |
| **WHO** | 고객담당자 (티켓 등록/승인), 지원담당자 (티켓 처리), 관리자 (전체 관리/수정 권한) |
| **RISK** | 근무시간 계산 엔진 정확성(전 스케줄러 의존), 티켓 상태 전이 로직 복잡도, 배치 스케줄러 간 경합/순서, 완료요청 3회 자동승인 로직 |
| **SUCCESS** | 티켓 전 구간 정상 동작 / 처리기한 자동 관리 / 완료요청 에스컬레이션 정상 동작 / RBAC 완전 적용 / Beta 50팀, 활성화율 60% |
| **SCOPE** | 마스터 관리 + 프로젝트 관리 + 티켓 워크플로우 + 요청/승인 + 만족도 평가 + 대시보드 + 아카이빙 + 온보딩 마법사 |

---

## Executive Summary

| 관점 | 내용 |
|------|------|
| **Problem** | 한국 중소/중견 기업은 글로벌 서비스데스크 솔루션의 불완전한 한국어 지원, 카카오톡 알림 부재, 계층형 승인 구조 미지원으로 서비스 운영에 마찰을 겪고 있다. |
| **Solution** | Option C (Pragmatic Balance) 아키텍처를 채택하여, 비즈니스 로직을 API Routes에 직접 구현하고 lib/ 유틸리티로 공유하는 실용적 구조로 고객사-프로젝트-티켓 연계 구조와 한국형 승인 워크플로우를 구현한다. |
| **Functional UX Effect** | 고객담당자는 배정된 프로젝트에서 바로 티켓을 등록하고 처리 현황을 실시간 확인하며, 지원담당자는 티켓 중심 업무 화면에서 접수-처리-완료 흐름을 효율적으로 처리한다. |
| **Core Value** | 고객사·프로젝트·담당자 연계 구조와 정교한 티켓 상태 워크플로우(자동접수, 연기/완료 승인, 만족도 평가)로 서비스 신뢰도를 정량적으로 관리한다. |

---

## V1.1 → V2.0 주요 변경사항

| # | 변경 항목 | V1.1 | V2.0 | 변경 근거 |
|---|----------|------|------|----------|
| 1 | SatisfactionRating.userId | NOT NULL | **nullable (`String?`)** | 아키텍처: 자동종료 시 User 없음 |
| 2 | SatisfactionRating 필드 | — | **`reminderSentAt DateTime?` 추가** | 아키텍처: 리마인더 중복 방지 |
| 3 | Ticket 필드 | — | **`lastEscalationAt DateTime?` 추가** | 아키텍처: 에스컬레이션 중복 방지 |
| 4 | CompleteRequest 필드 | — | **`previousStatus TicketStatus` 추가** | 분석설계: 반려 복귀 상태 추적 |
| 5 | NotificationType enum | 18개 | **21개** (+EXTEND_AUTO_APPROVE_SOON, EXTEND_AUTO_APPROVED, BATCH_JOB_FAILED) | UIUX+아키텍처 |
| 6 | Notification 인덱스 | (userId, isRead, createdAt) | **(userId, isDeleted, isRead, createdAt)** | 아키텍처: isDeleted 포함 |
| 7 | LoginHistory 인덱스 | (loginId, createdAt) | **(loginId, success, createdAt)** | 분석설계: 실패 분석 최적화 |
| 8 | CompleteRequest 인덱스 | (ticketId, attemptNumber) | **+ (status, createdAt)** 추가 | 분석설계: 자동승인 배치 최적화 |
| 9 | 배치 잡 수 | 7개 | **9개** (notification-cleanup, push-cleanup) | 아키텍처: 무제한 성장 방지 |
| 10 | 배치 재시도 | 3회 | **3회 exponential backoff + DLQ** | 아키텍처 |
| 11 | API 오류 응답 | 기본 형식 | **fieldErrors 포함 + 비즈니스 코드** | 분석설계 |
| 12 | 디렉토리 | — | **`app/(main)/profile/`, `app/(main)/notifications/`** 추가 | 아키텍처: 누락 페이지 |
| 13 | API 엔드포인트 | — | **`/api/profile` (GET, PUT)** 추가 | 아키텍처: 누락 API |
| 14 | role_hint 쿠키 | 서명 없음 | **HMAC 서명 적용** | 아키텍처: 변조 방어 |
| 15 | 낙관적 락 | Prisma 조건부 UPDATE | **$executeRaw RETURNING * 패턴** | 아키텍처: silent failure 방지 |
| 16 | 디자인 토큰 | 상태 색상만 | **우선순위 색상 + 폰트 + 간격** 추가 | UIUX |
| 17 | 모바일 레이아웃 | 미정의 | **사이드바→하단탭, 고정 액션바** 명시 | UIUX |
| 18 | 관리자 수정 fieldName | 미정의 | **AdminEditField enum 7종** 추가 | 분석설계 |
| 19 | 댓글 10분 수정 | 클라이언트만 | **서버 createdAt 기준 검증 명시** | 아키텍처: 우회 방지 |
| 20 | Presigned URL 만료 | "5분" 텍스트 | **업로드 5분, 다운로드 1시간** 명시 | 분석설계 |

---

## V2.0 → V2.1 주요 변경사항 [V2.1]

| # | 변경 항목 | V2.0 | V2.1 | 변경 근거 |
|---|----------|------|------|----------|
| 1 | UI 프레임워크 확정 | shadcn/ui + Tailwind 참조 잔존 | **Bootstrap 5 + React-Bootstrap 완전 전환** — SCSS 테마, 컴포넌트 매핑, 모바일 패턴 구체화 | 사용자 요구 + UIUX 전문가 |
| 2 | ExtendRequest.isDeleted | 미정의 | **Boolean @default(false)** 필드 추가 — unique 제약과 soft-delete 연동 | 아키텍처 |
| 3 | DLQ 재처리 API | 알림만 | **GET /api/admin/jobs + POST /api/admin/jobs/[jobId]/retry** | 아키텍처 |
| 4 | ticket-workflow.ts | 목적만 기술 | **5개 함수 인터페이스 정의** | 아키텍처 |
| 5 | CSRF | "검토" | **SameSite=Strict + Origin 헤더 검증 확정** | 아키텍처 |
| 6 | ROLE_HINT_SECRET | SESSION_SECRET 공유 | **별도 환경변수** 분리 | 아키텍처 |
| 7 | Graceful Shutdown | 미정의 | **SIGTERM + Worker.close() + 30초 대기** | 운영관리 |
| 8 | 비밀번호 변경 세션 | 미정의 | **전체 세션 폐기 + 재로그인** | 사용자 결정 |
| 9 | 배치 잡 | 9개 | **10개** (login-history-cleanup) | 분석설계 |
| 10 | Server/Client 경계 | 미정의 | **컴포넌트별 'use client' 규칙** 명시 | 아키텍처 |
| 11 | 페이지별 Bootstrap 컴포넌트 | 미정의 | **13개 페이지 컴포넌트 명세** | UIUX |
| 12 | 접근성 체크리스트 | 미정의 | **WCAG 2.1 AA 구체 항목 12종** | UIUX |
| 13 | SCSS 테마 | CSS 변수만 | **Bootstrap $variables + 커스텀 _status-colors.scss** | UIUX+개발 |
| 14 | 색상 대비 보정 | cancelled gray-500 | **gray-700으로 보정** (WCAG AA 4.5:1) | UIUX |

---

## 1. Architecture Overview

> V1.1과 동일. 변경 없음.

### 1.1 선택 아키텍처: Option C — Pragmatic Balance

비즈니스 로직이 API Routes에 직접 구현되거나 lib/ 유틸리티를 통해 처리되는 실용적 구조이다.

- **API Routes** (controller + business logic): 요청 검증, 도메인 로직, 상태 전이 조정, 트랜잭션 관리
- **lib/** (shared utilities): State Machine, Business Hours Engine, 세션, 채번 등 공유 유틸리티
- **lib/ticket-workflow.ts** [V2.0 추가]: 연기/완료 승인 등 API Route와 배치 잡이 공유하는 워크플로우 로직 격리
- **jobs/** (batch scheduler): BullMQ 기반 **9개** 배치 잡

### 1.2 핵심 설계 결정

| 결정 사항 | 선택 | 근거 |
|-----------|------|------|
| 상태 관리 | State Machine 패턴 (centralized) | 9개 상태, 17개 전이 이벤트 — 중앙 집중식 관리로 일관성 보장 |
| 배치 스케줄러 | BullMQ + Redis | 지속 큐, 재시도(exponential backoff), 스케줄링, 순차 실행, **DLQ** [V2.0] |
| 세션 관리 | Server Session + Redis | HttpOnly 쿠키 + Sliding Expiry 8시간 |
| 파일 저장소 | Cloudflare R2 (Presigned URL) | S3 호환 API, 비용 효율적 |
| Push 알림 | Web Push (VAPID) | Phase 1 즉시 제공 가능, Service Worker 기반 |
| 낙관적 락 | **$executeRaw RETURNING *** [V2.0] | Prisma 조건부 UPDATE의 silent failure 방지 |
| 워크플로우 공유 | **lib/ticket-workflow.ts** [V2.0] | API Route와 배치 잡 간 승인/반려 로직 중복 방지 |

### 1.3 시스템 아키텍처 다이어그램

> V1.1과 동일. 변경 없음.

---

## 2. Directory Structure [V2.0 개정]

```
nu-servicedesk/
├── app/                                    # Next.js App Router
│   ├── layout.tsx                          # Root layout
│   ├── page.tsx                            # Root redirect
│   ├── globals.css                         # 디자인 토큰 CSS 변수 [V2.0]
│   │
│   ├── (auth)/                             # 미인증 레이아웃
│   │   ├── login/
│   │   │   └── page.tsx                    # 로그인 페이지
│   │   └── change-password/
│   │       └── page.tsx                    # 초기 비밀번호 강제 변경
│   │
│   ├── (main)/                             # 통합 인증 레이아웃 (Header + Sidebar + Content)
│   │   ├── layout.tsx
│   │   │
│   │   ├── dashboard/
│   │   │   └── page.tsx                   # 통합 대시보드 (역할별 컴포넌트 분기)
│   │   │
│   │   ├── tickets/                        # 티켓 관리
│   │   │   ├── page.tsx                   # 티켓 목록
│   │   │   ├── new/
│   │   │   │   └── page.tsx               # 티켓 등록
│   │   │   └── [id]/
│   │   │       └── page.tsx               # 티켓 상세
│   │   │
│   │   ├── master/                         # 마스터 관리 (admin 전용)
│   │   │   ├── companies/
│   │   │   │   ├── page.tsx
│   │   │   │   └── [id]/
│   │   │   │       └── page.tsx
│   │   │   ├── users/
│   │   │   │   ├── page.tsx
│   │   │   │   └── [id]/
│   │   │   │       └── page.tsx
│   │   │   ├── projects/
│   │   │   │   ├── page.tsx
│   │   │   │   └── [id]/
│   │   │   │       └── page.tsx
│   │   │   ├── categories/
│   │   │   │   └── page.tsx
│   │   │   └── holidays/
│   │   │       └── page.tsx
│   │   │
│   │   ├── system/                         # 시스템 (admin 전용)
│   │   │   └── settings/
│   │   │       └── page.tsx
│   │   │
│   │   ├── profile/                        # [V2.0 추가] 내 정보
│   │   │   └── page.tsx
│   │   │
│   │   └── notifications/                  # [V2.0 추가] 알림 전체 목록
│   │       └── page.tsx
│   │
│   └── api/                                # API Routes
│       ├── auth/
│       │   ├── login/route.ts
│       │   ├── logout/route.ts
│       │   ├── session/route.ts
│       │   └── password/route.ts
│       ├── profile/                        # [V2.0 추가]
│       │   └── route.ts                   # GET, PUT
│       ├── companies/
│       │   ├── route.ts
│       │   └── [id]/
│       │       ├── route.ts
│       │       ├── deactivate/route.ts
│       │       └── departments/
│       │           ├── route.ts
│       │           └── [deptId]/route.ts
│       ├── users/
│       │   ├── route.ts
│       │   └── [id]/
│       │       ├── route.ts
│       │       └── reset-password/route.ts
│       ├── projects/
│       │   ├── route.ts
│       │   └── [id]/
│       │       ├── route.ts
│       │       └── members/route.ts
│       ├── categories/
│       │   ├── route.ts
│       │   └── [id]/route.ts
│       ├── holidays/
│       │   ├── route.ts
│       │   └── [id]/route.ts
│       ├── settings/
│       │   └── supervisor/route.ts
│       ├── tickets/
│       │   ├── route.ts
│       │   └── [id]/
│       │       ├── route.ts
│       │       ├── confirm/route.ts
│       │       ├── receive/route.ts
│       │       ├── cancel/route.ts
│       │       ├── extend/
│       │       │   ├── route.ts
│       │       │   ├── approve/route.ts
│       │       │   └── reject/route.ts
│       │       ├── complete/
│       │       │   ├── route.ts
│       │       │   ├── approve/route.ts
│       │       │   └── reject/route.ts
│       │       ├── rate/route.ts
│       │       ├── admin/route.ts
│       │       ├── assign/
│       │       │   ├── route.ts
│       │       │   └── [userId]/route.ts
│       │       └── comments/
│       │           ├── route.ts
│       │           └── [commentId]/route.ts
│       ├── attachments/
│       │   ├── presign/route.ts
│       │   └── [id]/route.ts
│       ├── notifications/
│       │   ├── route.ts
│       │   ├── unread-count/route.ts
│       │   ├── read-all/route.ts
│       │   └── [id]/
│       │       ├── read/route.ts
│       │       └── route.ts
│       ├── push-subscriptions/route.ts
│       ├── dashboard/route.ts
│       └── health/route.ts
│
├── jobs/                                   # BullMQ Batch Jobs (9개) [V2.0: +2]
│   ├── queue.ts                           # BullMQ 큐 초기화 + DLQ 설정 [V2.0]
│   ├── worker.ts                          # Worker 등록 + startup recovery
│   ├── auto-receive.job.ts
│   ├── delay-detect.job.ts
│   ├── extend-auto-approve.job.ts         # [V2.0] 사전 경고 알림 추가
│   ├── satisfaction-close.job.ts          # [V2.0] reminderSentAt 중복 체크
│   ├── project-deactivate-notify.job.ts
│   ├── customer-zero-warning.job.ts       # [V2.0] 24시간 중복 방지
│   ├── stale-escalation.job.ts            # [V2.0] lastEscalationAt 중복 방지
│   ├── notification-cleanup.job.ts        # [V2.0 추가] 90일 초과 알림 삭제
│   └── push-cleanup.job.ts               # [V2.0 추가] 90일 미사용 구독 삭제
│
├── lib/                                    # Shared Utilities
│   ├── prisma.ts
│   ├── redis.ts
│   ├── session.ts
│   ├── business-hours.ts
│   ├── ticket-state-machine.ts
│   ├── ticket-workflow.ts                 # [V2.0 추가] 승인/반려 공유 워크플로우
│   ├── ticket-number.ts
│   ├── ticket-constants.ts
│   ├── password.ts
│   ├── errors.ts                          # [V2.0] 비즈니스 오류 코드 체계
│   ├── logger.ts
│   ├── r2.ts
│   ├── push-notify.ts
│   ├── constants.ts
│   └── __tests__/
│       ├── business-hours.test.ts         # 50+ 테스트
│       └── ticket-state-machine.test.ts   # [V2.0 추가]
│
├── middleware/
│   └── middleware.ts                      # [V2.0] role_hint HMAC 서명 검증
│
├── components/
│   ├── layout/
│   │   ├── header.tsx
│   │   ├── sidebar.tsx                    # [V2.0] 모바일 하단탭 분기
│   │   ├── mobile-bottom-nav.tsx          # [V2.0 추가] 모바일 하단 네비게이션
│   │   ├── notification-bell.tsx
│   │   └── onboarding-checklist.tsx       # [V2.0 추가] 온보딩 체크리스트
│   └── ui/
│       ├── skeleton.tsx
│       ├── status-badge.tsx               # [V2.0 추가] 상태 배지 (색상+텍스트+아이콘)
│       ├── priority-badge.tsx             # [V2.0 추가] 우선순위 배지
│       └── mobile-action-bar.tsx          # [V2.0 추가] 모바일 하단 고정 액션
│
├── hooks/
│   └── use-media-query.ts                 # [V2.0 추가] 반응형 분기
│
├── types/
│   └── auth.ts
│
├── prisma/
│   ├── schema.prisma                      # [V2.0] 스키마 변경 반영
│   ├── seed.ts
│   └── migrations/
│       ├── 20260408234417_init/
│       ├── 20260409043401_add_attachment_uploader/
│       ├── 20260409081801_make_user_email_phone_optional/
│       └── 20260409XXXXXX_v2_schema_updates/  # [V2.0 마이그레이션]
│
├── instrumentation.ts
└── docker-compose.yml
```

---

## 3. Database Schema — V2.0 변경분

> 전체 스키마는 V1.1 기반. 아래는 **V2.0에서 변경/추가된 부분만** 기술.

### 3.1 SatisfactionRating — userId nullable + reminderSentAt [V2.0]

```prisma
model SatisfactionRating {
  id          String    @id @default(cuid())
  ticketId    String    @unique @map("ticket_id")
  userId      String?   @map("user_id")                     // [V2.0] nullable (자동종료 시 null)
  rating      Int?
  comment     String?   @db.Text
  autoCompleted Boolean @default(false) @map("auto_completed")
  reminderSentAt DateTime? @map("reminder_sent_at")          // [V2.0] 리마인더 발송 일시
  createdAt   DateTime  @default(now()) @map("created_at")

  ticket      Ticket   @relation(fields: [ticketId], references: [id])
  user        User?    @relation(fields: [userId], references: [id])  // [V2.0] optional

  @@map("satisfaction_ratings")
}
```

### 3.2 Ticket — lastEscalationAt [V2.0]

```prisma
// Ticket 모델에 추가
lastEscalationAt    DateTime? @map("last_escalation_at")    // [V2.0] 에스컬레이션 중복 방지
```

### 3.3 CompleteRequest — previousStatus [V2.0]

```prisma
model CompleteRequest {
  // ... 기존 필드 유지 ...
  previousStatus  TicketStatus  @map("previous_status")      // [V2.0] 반려 시 복귀 상태

  // [V2.0] 인덱스 추가
  @@index([ticketId, attemptNumber])
  @@index([status, createdAt])                                // [V2.0] 자동승인 배치 최적화
  @@map("complete_requests")
}
```

### 3.4 NotificationType enum [V2.0]

```prisma
enum NotificationType {
  // ... 기존 18개 유지 ...
  EXTEND_AUTO_APPROVE_SOON   // [V2.0] 연기 자동승인 사전 경고
  EXTEND_AUTO_APPROVED       // [V2.0] 연기 자동승인 완료
  BATCH_JOB_FAILED           // [V2.0] 배치 잡 DLQ 실패 알림
}
```

### 3.5 AdminEditField enum [V2.0 추가]

```prisma
enum AdminEditField {
  TITLE
  CONTENT
  CATEGORY
  PRIORITY
  ASSIGNEE
  STATUS
  DEADLINE
}
```

### 3.6 Notification 인덱스 변경 [V2.0]

```prisma
// 기존: @@index([userId, isRead, createdAt])
@@index([userId, isDeleted, isRead, createdAt])  // [V2.0] isDeleted 포함
```

### 3.7 LoginHistory 인덱스 변경 [V2.0]

```prisma
// 기존: @@index([loginId, createdAt])
@@index([loginId, success, createdAt])  // [V2.0] 실패 분석 최적화
```

---

## 4. API Design — V2.0 변경분

> 전체 API는 V1.1 기반. 아래는 **V2.0에서 변경/추가된 부분만** 기술.

### 4.1 공통 오류 응답 형식 [V2.0 개정]

```typescript
interface ApiResponse<T> {
  success: boolean;
  data?: T;
  error?: {
    code: string;
    message: string;
    status: number;
    fieldErrors?: Record<string, string[]>;  // [V2.0] Zod 유효성 검증 실패 시
  };
}
```

### 4.2 Profile API [V2.0 추가]

| Method | Path | Request Body | Response | 인증 | 역할 |
|--------|------|-------------|----------|:----:|------|
| GET | `/api/profile` | - | `User` (본인 정보) | O | 모두 |
| PUT | `/api/profile` | `{ name?, email?, phone? }` | `User` | O | 모두 |

### 4.3 confirm API — 409 응답 명시 [V2.0]

```typescript
// POST /api/tickets/[id]/confirm
// RECEIVED 상태가 아닌 경우:
// - 이미 DELAYED로 전환됨 → 409 { code: "TICKET_ALREADY_DELAYED" }
// - 이미 IN_PROGRESS → 204 No Content (멱등성)

// 구현: $executeRaw RETURNING * 사용
const result = await prisma.$executeRaw`
  UPDATE tickets SET status = ${newStatus}, updated_at = NOW()
  WHERE id = ${ticketId} AND status = 'RECEIVED'
  RETURNING *
`;
if (result.length === 0) {
  // 현재 상태 조회 후 적절한 응답
  const ticket = await prisma.ticket.findUnique({ where: { id: ticketId } });
  if (ticket?.status === 'DELAYED') throw new ConflictError('TICKET_ALREADY_DELAYED');
  if (ticket?.status === 'IN_PROGRESS') return NextResponse.json(null, { status: 204 });
}
```

### 4.4 댓글 수정 서버 검증 [V2.0]

```typescript
// PUT /api/tickets/[id]/comments/[commentId]
// 서버에서 createdAt 기준 10분 이내 검증 (클라이언트 우회 방지)
const comment = await prisma.comment.findUnique({ where: { id: commentId } });
const elapsed = differenceInMinutes(new Date(), comment.createdAt);
if (elapsed > 10) throw new BusinessError('COMMENT_EDIT_EXPIRED', 422);
```

---

## 5. State Machine Design — V2.0 변경분

> 전체 State Machine은 V1.1 기반. 아래는 변경/추가분만.

### 5.1 전이 테이블 변경 사항

**REJECT_COMPLETE 이벤트 — previousStatus 기반 복귀:**

| From State | Event | To State | Guard Conditions | Side Effects |
|-----------|-------|----------|-----------------|--------------|
| COMPLETE_REQUESTED | `REJECT_COMPLETE` | **CompleteRequest.previousStatus** | 고객담당자, attempt <= 2 | 2회차 반려 시 관리책임자 알림. **previousStatus로 복귀** 후 즉시 기한 재확인 |

### 5.2 낙관적 락 구현 패턴 [V2.0]

```typescript
// Prisma 조건부 UPDATE 대신 $executeRaw RETURNING * 사용
// 이유: Prisma의 조건 불일치 시 null 반환(silent failure) 방지

async function transitionTicketStatus(
  ticketId: string,
  expectedStatus: TicketStatus,
  newStatus: TicketStatus
): Promise<Ticket | null> {
  const result = await prisma.$queryRaw<Ticket[]>`
    UPDATE tickets
    SET status = ${newStatus}::text::"TicketStatus",
        updated_at = NOW()
    WHERE id = ${ticketId} AND status = ${expectedStatus}::text::"TicketStatus"
    RETURNING *
  `;
  return result[0] ?? null; // null이면 경합 패배
}
```

---

## 6. Batch Job Design — V2.0 변경분

### 6.1 BullMQ 공통 설정 [V2.0]

```typescript
// jobs/queue.ts — V2.0: exponential backoff + DLQ
const defaultJobOptions: JobsOptions = {
  attempts: 3,
  backoff: { type: 'exponential', delay: 2000 },  // [V2.0] 2초, 4초, 8초
  removeOnComplete: { count: 100 },
  removeOnFail: false,  // DLQ에 보존
};

// DLQ 처리: Worker의 'failed' 이벤트에서 최종 실패 감지
worker.on('failed', async (job, err) => {
  if (job && job.attemptsMade >= 3) {
    // DLQ: 관리자 알림 발송
    await sendNotificationToAdmins({
      type: 'BATCH_JOB_FAILED',
      title: `배치 잡 실패: ${job.name}`,
      body: `${err.message} (${job.attemptsMade}회 재시도 후 실패)`,
    });
  }
});
```

### 6.2 Job 3 변경: extend-auto-approve — 사전 경고 [V2.0]

```typescript
// jobs/extend-auto-approve.job.ts — V2.0: 사전 경고 알림 추가
async function extendAutoApproveJob() {
  const now = new Date();
  const pendingRequests = await prisma.extendRequest.findMany({
    where: { status: 'PENDING' },
    include: { ticket: true },
  });

  for (const req of pendingRequests) {
    const elapsed = businessHoursEngine.calculateBusinessHours(req.createdAt, now);

    // [V2.0] 3근무시간 경과 (자동승인 1시간 전) → 사전 경고
    if (elapsed >= 3 && elapsed < 4) {
      // 이미 발송했는지 체크 (알림 DB에서 EXTEND_AUTO_APPROVE_SOON 존재 확인)
      const alreadySent = await prisma.notification.findFirst({
        where: { ticketId: req.ticketId, type: 'EXTEND_AUTO_APPROVE_SOON' },
      });
      if (!alreadySent) {
        await sendNotification({
          type: 'EXTEND_AUTO_APPROVE_SOON',
          title: '연기요청 자동 승인 예정',
          body: '1근무시간 내 반려하지 않으면 자동 승인됩니다.',
        });
      }
    }

    // 4근무시간 경과 → 자동 승인 (기존 로직)
    if (elapsed >= 4) {
      await ticketWorkflow.approveExtend(req, { autoApproved: true });
    }
  }
}
```

### 6.3 Job 4 변경: satisfaction-close — 리마인더 중복 방지 [V2.0]

```typescript
// satisfaction-close.job.ts — V2.0: reminderSentAt 체크
// 4근무일 경과 시 리마인더 발송
if (elapsedDays >= 4 && elapsedDays < 5) {
  const rating = await prisma.satisfactionRating.findUnique({
    where: { ticketId: ticket.id },
  });
  if (rating && !rating.reminderSentAt) {  // [V2.0] 중복 방지
    await sendNotification({ type: 'SATISFACTION_REMINDER', ... });
    await prisma.satisfactionRating.update({
      where: { id: rating.id },
      data: { reminderSentAt: new Date() },  // [V2.0]
    });
  }
}
```

### 6.4 Job 7 변경: stale-escalation — 중복 방지 [V2.0]

```typescript
// stale-escalation.job.ts — V2.0: lastEscalationAt 체크
const staleTickets = await prisma.ticket.findMany({
  where: {
    status: 'DELAYED',
    // 마지막 에스컬레이션 24시간 이내 건 제외 [V2.0]
    OR: [
      { lastEscalationAt: null },
      { lastEscalationAt: { lt: subHours(new Date(), 24) } },
    ],
  },
});

for (const ticket of staleTickets) {
  const delayedDays = businessHoursEngine.calculateBusinessDays(/* ... */);
  if (delayedDays >= 3) {
    await sendNotification({ type: 'STALE_ESCALATION', ... });
    await prisma.ticket.update({
      where: { id: ticket.id },
      data: { lastEscalationAt: new Date() },  // [V2.0]
    });
  }
}
```

### 6.5 Job 8: notification-cleanup [V2.0 추가]

```typescript
// jobs/notification-cleanup.job.ts — 매일 03:00
async function notificationCleanupJob() {
  const cutoff = subDays(new Date(), 90);
  const { count } = await prisma.notification.deleteMany({
    where: { createdAt: { lt: cutoff } },
  });
  logger.info(`notification-cleanup: ${count}건 삭제`);
}
```

### 6.6 Job 9: push-cleanup [V2.0 추가]

```typescript
// jobs/push-cleanup.job.ts — 매일 03:00
async function pushCleanupJob() {
  const cutoff = subDays(new Date(), 90);
  const { count } = await prisma.pushSubscription.deleteMany({
    where: { lastUsedAt: { lt: cutoff } },
  });
  logger.info(`push-cleanup: ${count}건 삭제`);
}
```

### 6.7 배치 잡 요약 (9개) [V2.0]

| # | 잡 | 주기 | backoff | DLQ | 중복 방지 |
|---|-----|------|---------|:---:|----------|
| 1 | auto-receive | 1분 | exp 2s | ✅ | WHERE status='REGISTERED' |
| 2 | delay-detect | 1분 (체인) | exp 2s | ✅ | WHERE status IN(...) |
| 3 | extend-auto-approve | 1분 | exp 2s | ✅ | +사전 경고 알림 DB 체크 |
| 4 | satisfaction-close | 1시간 | exp 2s | ✅ | **reminderSentAt** |
| 5 | project-deactivate | 매일 00:00 | exp 2s | ✅ | — |
| 6 | customer-zero-warning | 매일 09:00 | exp 2s | ✅ | **24시간 중복 체크** |
| 7 | stale-escalation | 매일 09:00 | exp 2s | ✅ | **lastEscalationAt** |
| 8 | notification-cleanup | 매일 03:00 | exp 2s | ✅ | — |
| 9 | push-cleanup | 매일 03:00 | exp 2s | ✅ | — |

---

## 7. Authentication — V2.0 변경분

### 7.1 role_hint 쿠키 HMAC 서명 [V2.0]

```typescript
// lib/session.ts — V2.0: role_hint 서명 적용
import { createHmac } from 'crypto';

function signRoleHint(role: string, secret: string): string {
  const signature = createHmac('sha256', secret).update(role).digest('hex').slice(0, 16);
  return `${role}.${signature}`;
}

function verifyRoleHint(value: string, secret: string): string | null {
  const [role, sig] = value.split('.');
  const expected = createHmac('sha256', secret).update(role).digest('hex').slice(0, 16);
  return sig === expected ? role : null;
}

// 로그인 시: Set-Cookie: role_hint={role}.{signature}; HttpOnly; Secure; SameSite=Strict
// middleware.ts에서: verifyRoleHint(roleHint, SESSION_SECRET)
```

---

## 8. Notification System — V2.0 변경분

### 8.1 알림 이벤트 21종 [V2.0: +3]

> V1.1의 18종 + 3종 추가

| # | NotificationType | 수신 대상 | 트리거 |
|---|-----------------|---------|--------|
| 19 | `EXTEND_AUTO_APPROVE_SOON` | 고객담당자 | 자동승인 1근무시간 전 |
| 20 | `EXTEND_AUTO_APPROVED` | 지원담당자 + 등록자 | 연기 자동승인 완료 |
| 21 | `BATCH_JOB_FAILED` | 전체 admin | 배치 잡 DLQ 최종 실패 |

---

## 9. File Upload Design

> V1.1과 동일. **Presigned URL 만료 시간만 명시 [V2.0]:**

| 용도 | TTL |
|------|-----|
| 업로드 | 5분 |
| 다운로드 | 1시간 |

---

## 10. UI Page Structure — V2.0 변경분

### 10.1 역할별 사이드바 메뉴 [V2.0 개정]

| 메뉴 항목 | Route | admin | support | customer |
|----------|-------|:-----:|:-------:|:--------:|
| 대시보드 | `/dashboard` | ✅ | ✅ | ✅ |
| 티켓 목록 | `/tickets` | ✅ | ✅ | ✅ |
| 티켓 등록 | `/tickets/new` | ❌ | ✅ | ✅ |
| 고객사 | `/master/companies` | ✅ | ❌ | ❌ |
| 사용자 | `/master/users` | ✅ | ❌ | ❌ |
| 프로젝트 | `/master/projects` | ✅ | ❌ | ❌ |
| 카테고리 | `/master/categories` | ✅ | ❌ | ❌ |
| 공휴일 | `/master/holidays` | ✅ | ❌ | ❌ |
| 설정 | `/system/settings` | ✅ | ❌ | ❌ |
| **내 정보** | **`/profile`** | ✅ | ✅ | ✅ |
| **알림 전체** | **`/notifications`** | ✅ | ✅ | ✅ |

### 10.2 모바일 레이아웃 [V2.0 추가]

```
Desktop (>= 768px):
┌──────────────────────────────────────────────────────────────┐
│  [Header] Logo | 알림벨(unread) | 사용자명/역할 | 로그아웃      │
├──────────┬───────────────────────────────────────────────────┤
│[Sidebar] │  [Main Content]                                    │
│ 대시보드 │  Breadcrumb                                        │
│ 티켓     │  페이지 컨텐츠                                      │
│ ...      │                                                    │
└──────────┴───────────────────────────────────────────────────┘

Mobile (< 768px):
┌──────────────────────────────────────────┐
│ [Header] Logo | 알림벨 | 사용자 아이콘     │
├──────────────────────────────────────────┤
│                                          │
│  [Main Content]                          │
│  전체 너비 사용                            │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │ [Fixed Bottom Action Bar]          │  │  ← 승인/반려/완료요청 등
│  │ [승인] [반려]                        │  │     핵심 액션 하단 고정
│  └────────────────────────────────────┘  │
├──────────────────────────────────────────┤
│ [Bottom Tab Nav]                         │
│ 🏠대시보드 | 🎫티켓 | 🔔알림 | ⚙️설정     │  ← 사이드바 대체
└──────────────────────────────────────────┘
```

**모바일 하단 탭 메뉴:**

| 탭 | Route | 아이콘 |
|----|-------|--------|
| 대시보드 | `/dashboard` | 🏠 |
| 티켓 | `/tickets` | 🎫 |
| 알림 | `/notifications` | 🔔 (미읽음 뱃지) |
| 설정 | `/profile` (customer/support) 또는 `/system/settings` (admin) | ⚙️ |

### 10.3 디자인 토큰 [V2.0 확장]

#### 티켓 상태 색상 (V1.1과 동일)

| 상태 | 색상 (Tailwind) | 배경 | 아이콘 [V2.0] |
|------|----------------|------|:-------------:|
| REGISTERED | `text-amber-600` | `bg-amber-50` | ○ (원) |
| RECEIVED | `text-blue-600` | `bg-blue-50` | ◷ (시계) |
| IN_PROGRESS | `text-green-600` | `bg-green-50` | ▶ (재생) |
| DELAYED | `text-red-600` | `bg-red-50` + pulse | ⚠ (경고) |
| EXTEND_REQUESTED | `text-orange-600` | `bg-orange-50` | ⏸ (일시정지) |
| COMPLETE_REQUESTED | `text-indigo-600` | `bg-indigo-50` | ✓ (체크) |
| SATISFACTION_PENDING | `text-purple-600` | `bg-purple-50` | ★ (별) |
| CLOSED | `text-gray-500` | `bg-gray-100` | ● (원) |
| CANCELLED | `text-gray-400` | `bg-gray-50` + strikethrough | ✕ (엑스) |

> **[V2.0]** 아이콘 추가 — 색각 이상 사용자를 위해 색상+텍스트+아이콘 3중 표현 (WCAG 2.1 AA)

#### 우선순위 색상 [V2.0 추가]

| 우선순위 | 색상 | 배경 |
|---------|------|------|
| URGENT (긴급) | `text-red-700` | `bg-red-100` |
| HIGH (높음) | `text-orange-600` | `bg-orange-50` |
| NORMAL (보통) | `text-blue-600` | `bg-blue-50` |
| LOW (낮음) | `text-gray-500` | `bg-gray-100` |

#### 폰트/간격 CSS 변수 [V2.0 추가]

```css
/* globals.css */
:root {
  /* 폰트 */
  --font-sans: 'Pretendard Variable', -apple-system, sans-serif;

  /* 간격 */
  --spacing-page: 1.5rem;     /* 페이지 패딩 */
  --spacing-card: 1rem;       /* 카드 내부 패딩 */
  --spacing-section: 2rem;    /* 섹션 간 간격 */

  /* 반경 */
  --radius-sm: 0.375rem;
  --radius-md: 0.5rem;
  --radius-lg: 0.75rem;

  /* 날짜 표시 형식 */
  /* 한국 사용자 기준: "2026.04.09(수)" 또는 "04.09 10:30" */
}
```

---

## 11. Implementation Guide — V2.0 변경분

### 11.1 Module 구성 변경

> Module 1~12는 V1.1과 동일. 아래 변경사항만 추가.

**Module 2 (인증) 추가 사항:**
- role_hint HMAC 서명 적용
- FR-30 프로필 API 추가

**Module 6 (배치) 변경:**
- 근무시간 엔진 50+ 테스트를 Module 5 **전에** 선행 개발 [V2.0]
- BullMQ 공통 설정에 exponential backoff + DLQ 추가

**Module 7 (워크플로우) 변경:**
- lib/ticket-workflow.ts에 승인/반려 공유 로직 격리
- CompleteRequest.previousStatus 기반 반려 복귀
- extend-auto-approve: 사전 경고 알림 추가

**Module 10 (Push) 변경:**
- 알림 19개 → 21개 이벤트 (EXTEND_AUTO_APPROVE_SOON, EXTEND_AUTO_APPROVED, BATCH_JOB_FAILED)

**Module 12 (대시보드) 추가:**
- 온보딩 체크리스트 컴포넌트
- 모바일 레이아웃 (하단 탭 + 고정 액션바)
- 배치 잡 +2 (notification-cleanup, push-cleanup)

### 11.2 권장 개발 순서 변경 [V2.0]

```
Module 1: Docker + Prisma 스키마 (V2.0 마이그레이션 포함)
     ↓
근무시간 엔진 선행 개발 (50+ 테스트) ← [V2.0 순서 변경: Module 5 전에]
     ↓
Module 2: 인증 + 프로필 + role_hint 서명
     ↓
Module 3~4: 마스터/프로젝트 관리
     ↓
Module 5: 티켓 코어 + State Machine + 낙관적 락($executeRaw)
     ↓
Module 6: BullMQ (backoff + DLQ) + 배치 기초
     ↓
Module 7: 워크플로우 (ticket-workflow.ts + previousStatus + 사전 경고)
     ↓
Module 8~9: 댓글/파일/필터 (10분 수정 서버 검증)
     ↓
Module 10~11: Push 21개 이벤트 + 알림 센터 + /notifications 페이지
     ↓
Module 12: 대시보드 + 온보딩 + 모바일 레이아웃 + 배치 +2
```

---

## 12. Business Hours Engine Design

> V1.1과 동일. 변경 없음.

---

## 환경 변수

> V1.1과 동일. SESSION_SECRET은 role_hint HMAC 서명에도 사용 [V2.0 명시].

| 변수명 | 설명 | 용도 [V2.0 보강] |
|--------|------|-----------------|
| `SESSION_SECRET` | 세션 서명 키 (32자+) | 세션 ID 생성 전용 [V2.1: role_hint 분리] |
| `ROLE_HINT_SECRET` | role_hint 쿠키 HMAC 서명 전용 키 (32자+) [V2.1 추가] | role_hint 서명/검증 전용 — SESSION_SECRET과 독립 로테이션 가능 |
| 기타 | V1.1과 동일 | — |

---

## 부록: V1.1 → V2.0 변경사항 요약

| # | 구분 | 위치 | 변경 내용 |
|---|------|------|---------|
| 1 | **스키마** | SatisfactionRating.userId | `String` → **`String?`** (nullable) |
| 2 | **스키마** | SatisfactionRating | **`reminderSentAt DateTime?`** 필드 추가 |
| 3 | **스키마** | Ticket | **`lastEscalationAt DateTime?`** 필드 추가 |
| 4 | **스키마** | CompleteRequest | **`previousStatus TicketStatus`** 필드 추가 |
| 5 | **스키마** | NotificationType | 18개 → **21개** |
| 6 | **스키마** | AdminEditField | **enum 7종 추가** |
| 7 | **인덱스** | Notification | (userId, **isDeleted**, isRead, createdAt) |
| 8 | **인덱스** | LoginHistory | (loginId, **success**, createdAt) |
| 9 | **인덱스** | CompleteRequest | **(status, createdAt)** 추가 |
| 10 | **디렉토리** | pages | `/profile`, `/notifications` 페이지 추가 |
| 11 | **디렉토리** | API | `/api/profile` 엔드포인트 추가 |
| 12 | **디렉토리** | lib | **`ticket-workflow.ts`** 추가 |
| 13 | **디렉토리** | components | `mobile-bottom-nav`, `status-badge`, `priority-badge`, `mobile-action-bar`, `onboarding-checklist` 추가 |
| 14 | **배치 잡** | jobs | 7개 → **9개** (notification-cleanup, push-cleanup) |
| 15 | **배치** | 공통 설정 | **exponential backoff + DLQ** 추가 |
| 16 | **배치** | extend-auto-approve | **사전 경고 알림** 로직 추가 |
| 17 | **배치** | satisfaction-close | **reminderSentAt 중복 체크** |
| 18 | **배치** | stale-escalation | **lastEscalationAt 중복 체크** |
| 19 | **배치** | customer-zero-warning | **24시간 중복 방지** |
| 20 | **인증** | role_hint | **HMAC 서명 적용** |
| 21 | **API** | 오류 응답 | **fieldErrors + 비즈니스 오류 코드 체계** |
| 22 | **API** | confirm | **409 TICKET_ALREADY_DELAYED 응답** 명시 |
| 23 | **API** | 댓글 수정 | **서버 createdAt 기준 10분 검증** |
| 24 | **구현** | 낙관적 락 | **$executeRaw RETURNING * 패턴** |
| 25 | **UI** | 모바일 | **하단 탭 + 고정 액션바 레이아웃** |
| 26 | **UI** | 디자인 토큰 | 우선순위 색상, 아이콘, 폰트, 간격 변수 추가 |
| 27 | **UI** | Presigned URL | **업로드 5분, 다운로드 1시간** 명시 |

---

## V2.1 추가 섹션

### A. ExtendRequest.isDeleted 스키마 추가 [V2.1]

```prisma
model ExtendRequest {
  // ... 기존 필드 유지 ...
  isDeleted       Boolean             @default(false) @map("is_deleted")  // [V2.1] soft-delete
  // @@unique([ticketId]) 유지 — isDeleted=false인 건만 앱 레벨에서 유효성 체크
}
```

### B. ticket-workflow.ts 인터페이스 정의 [V2.1]

```typescript
// lib/ticket-workflow.ts
interface TicketWorkflow {
  approveExtend(request: ExtendRequest, opts: { autoApproved: boolean }): Promise<Ticket>;
  rejectExtend(request: ExtendRequest, reason: string, actorId: string): Promise<Ticket>;
  approveComplete(request: CompleteRequest, actorId: string): Promise<Ticket>;
  rejectComplete(request: CompleteRequest, reason: string, actorId: string): Promise<Ticket>;
  autoApproveComplete3rd(request: CompleteRequest): Promise<Ticket>;
}
// 각 함수: 트랜잭션(상태전이+이력+알림INSERT) → 커밋 → Push 발송(비동기)
// 에러: BusinessError throw → 호출자가 HTTP 응답 변환
```

### C. DLQ 재처리 Admin API [V2.1]

| Method | Path | Response | 인증 | 역할 |
|--------|------|----------|:----:|------|
| GET | `/api/admin/jobs` | `{ failed: JobInfo[], counts: { failed, waiting, active } }` | O | admin |
| POST | `/api/admin/jobs/[jobId]/retry` | `{ success, jobId }` | O | admin |

### D. Graceful Shutdown [V2.1]

```typescript
// instrumentation.ts
export async function register() {
  if (process.env.NEXT_RUNTIME === 'nodejs') {
    const { startWorkers, stopWorkers } = await import('./jobs/worker');
    startWorkers();
    process.on('SIGTERM', async () => {
      logger.info('SIGTERM received, graceful shutdown...');
      await stopWorkers(); // worker.close() + 진행 중 잡 완료 대기 (최대 30초)
      process.exit(0);
    });
  }
}
```

### E. 배치 잡 10번: login-history-cleanup [V2.1]

| # | 잡 | 주기 | backoff | DLQ | 중복 방지 |
|---|-----|------|---------|:---:|----------|
| 10 | login-history-cleanup | 매일 03:30 | exp 2s | ✅ | LIMIT 10000건/회 |

```typescript
// jobs/login-history-cleanup.job.ts
async function loginHistoryCleanupJob() {
  const cutoff = subYears(new Date(), 1);
  const result = await prisma.$executeRaw`
    DELETE FROM login_history WHERE created_at < ${cutoff} LIMIT 10000
  `;
  logger.info(`login-history-cleanup: ${result}건 삭제`);
}
```

### F. 비밀번호 변경 전체 세션 폐기 [V2.1]

```typescript
// PUT /api/auth/password 처리 후:
// 1. Redis에서 현재 사용자의 전체 세션 삭제
const sessionKeys = await redis.keys(`session:*`);
for (const key of sessionKeys) {
  const data = await redis.get(key);
  if (data && JSON.parse(data).userId === currentUserId) {
    await redis.del(key);
  }
}
// 2. 응답: { success: true, requireRelogin: true }
// 3. 클라이언트: 로그인 페이지 리다이렉트
```

### G. CSRF 확정 [V2.1]

Phase 1: **SameSite=Strict + API Routes에서 Origin 헤더 검증** 확정.
- `req.headers.get('origin')` vs `NEXT_PUBLIC_APP_URL` 비교
- 불일치 시 403 응답

### H. Server/Client Component 경계 [V2.1]

```
Server Component (기본 — Bootstrap CSS 클래스만):
├── app/(main)/*/page.tsx (데이터 페칭)
└── app/(main)/layout.tsx (그리드 구조)

Client Component ('use client' 필수):
├── components/layout/header.tsx (Navbar, Dropdown)
├── components/layout/sidebar.tsx (Offcanvas)
├── components/layout/notification-bell.tsx (Dropdown + Badge)
├── components/layout/mobile-bottom-nav.tsx (Nav + 라우팅)
├── components/layout/onboarding-checklist.tsx (Modal fullscreen)
├── components/ui/status-badge.tsx (Badge + CSS 변수)
├── components/ui/priority-badge.tsx (Badge)
├── components/ui/mobile-action-bar.tsx (ButtonGroup)
└── components/ui/confirm-dialog.tsx (Modal)
```

### I. 페이지별 Bootstrap 컴포넌트 명세 [V2.1]

| 페이지 | 주요 Bootstrap 컴포넌트 | 레이아웃 |
|--------|----------------------|---------|
| 로그인 | Card, Form, Form.Control, Button, Alert | Container(400px) 중앙 |
| 대시보드 | Card, Row/Col, Badge, Table, ProgressBar(온보딩) | Container-fluid + Row g-3 |
| 티켓 목록 | Table, Form.Control(검색), Form.Select(필터), Badge, Pagination, Offcanvas(모바일필터) | Container-fluid + Card |
| 티켓 등록 | Form, Form.Group, Form.Select, Button, Card | Container.lg 2컬럼 |
| 티켓 상세 | Card, Tabs/Tab, Badge, Button, Modal(승인/반려), ListGroup(이력) | Container.xl |
| 마스터 CRUD | Table, Modal, Form, Breadcrumb, Badge, Accordion(부서) | Container-fluid + Card |
| 프로필 | Card, Form, Button, ProgressBar(비밀번호 강도) | Container.sm |
| 알림 전체 | ListGroup, Badge, Tabs, Pagination | Container.md + Card |
| 온보딩 마법사 | Modal(fullscreen), ProgressBar, ListGroup, Form, Button | Modal.xl 단계별 |

### J. Bootstrap SCSS 테마 구성 [V2.1]

```scss
// styles/_variables.scss
$font-family-sans-serif: 'Pretendard Variable', -apple-system, sans-serif;
$font-size-base: 0.9375rem;  // 15px 한국어 가독성
$line-height-base: 1.6;
$primary: #3B5BDB;
$border-radius: 0.5rem;
$border-radius-sm: 0.375rem;
$border-radius-lg: 0.75rem;

// styles/bootstrap-custom.scss (진입점)
@import 'variables';
@import '~bootstrap/scss/bootstrap';
@import 'status-colors';

// styles/_status-colors.scss — 커스텀 상태/우선순위 색상
// 9개 상태 + 4개 우선순위를 CSS 변수로 정의 (globals.css의 :root 블록)
```

### K. 접근성 체크리스트 [V2.1]

| 항목 | 구현 방법 |
|------|---------|
| Skip Link | `<a href="#main-content" className="visually-hidden-focusable">` |
| StatusBadge | 색상+텍스트+아이콘 3중 표현 + `aria-label="상태: {label}"` |
| 알림 벨 | `<span className="visually-hidden">미읽음 알림 {count}개</span>` |
| MobileBottomNav | `aria-current="page"` 현재 탭 표시 |
| Fixed Action Bar | `role="toolbar"` + `aria-label="티켓 액션"` |
| 버튼 최소 크기 | 44x44px (WCAG 터치 타겟) |
| 색상 대비 | cancelled: gray-500→**gray-700** 보정 (4.5:1 이상) |
| Modal 포커스 트랩 | react-bootstrap Modal 내장 |
| 댓글 수정 타이머 | `aria-live="polite"` 잔여시간 고지 |
| 비밀번호 강도 | ProgressBar `aria-valuenow` + `aria-valuetext="강함"` |
| 온보딩 진행률 | ProgressBar `aria-label="온보딩 진행률"` |
| z-index 계층 | BottomNav(1031) > ActionBar(1030), Modal(1050)이 둘 다 가림. Modal 오픈 시 ActionBar display:none |

### L. 모바일 z-index 계층 관리 [V2.1]

| 레이어 | z-index | Bootstrap 기본 |
|--------|:-------:|:---------:|
| MobileBottomNav | 1031 | fixed(1030) 위 |
| MobileActionBar | 1030 | fixed |
| Modal backdrop | 1040 | 기본 |
| Modal | 1050 | 기본 |
| Offcanvas | 1045 | 기본 |

> Modal 오픈 시 MobileActionBar는 `display: none` 처리하여 backdrop 위 돌출 방지
